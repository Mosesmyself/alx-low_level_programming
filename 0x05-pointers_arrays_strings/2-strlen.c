#include "main.h"

/**
 * _strlen -  returns the length of a string.
 * @s: string.
 * Return: return the lenght.
 */
int _strlen(char *s)

{
	int lenght = 0;

	while (*str != '0')
	{
		lenght++;
		s++;
	}

	return (lenght0);
}
